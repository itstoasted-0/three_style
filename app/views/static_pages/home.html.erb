 <% if logged_in? %>
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <h3>投稿一覧 (<%= @feed_items.count %>)</h3>
        <%= render 'shared/feed' %>
      </div>
    </div>
  </div>
<% else %>
  <center>
    <h1>Welcom to Three-Style！</h1>
    <h2>横ノリの仲間を見つけませんか？</h2>
    <p>情報を繋ぐ場所</p>
  </center>
<% end %>