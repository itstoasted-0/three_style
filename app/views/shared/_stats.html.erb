<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>">
    <strong id="following" class="stat">フォロー：<%= @user.following.count %></strong>人
  </a>  
  <a href="<%= followers_user_path(@user) %>">
     | <strong id="followers" class="stat">フォロワー：<%= @user.followers.count %></strong>人
  </a>
</div>